import { LengthHintPipe } from './length-hint.pipe';

describe('LengthHintPipe', () => {
  it('create an instance', () => {
    const pipe = new LengthHintPipe();
    expect(pipe).toBeTruthy();
  });
});
